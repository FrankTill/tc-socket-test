# Authentication token for the WebSocket server (required)
TOKEN=your_authentication_token_here

# Logging level (optional, default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
